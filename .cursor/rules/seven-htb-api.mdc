---
description: HTB API connector and datastore sync patterns
globs: "{modules/htb-api.js,config/htb.js,models/SevenDatastore.js,models/api-classes.js}"
alwaysApply: false
---

# HTB API & Datastore

## Auth & bases

```javascript
// config/htb.js — never hardcode URLs outside this file
HTB_API_BASE    // v4 — default https://labs.hackthebox.com/api/v4
HTB_API_V5_BASE // v5 — machine list pagination
HTB_APP_BASE    // https://app.hackthebox.com — profile/badge URLs
```

Token: `HTB_V4_TOKEN` (OAuth access JWT) + `HTB_REFRESH_TOKEN` (refresh). Auto-refresh via `POST ${HTB_API_BASE}/login/refresh`; persist to `HTB_TOKEN_FILE` + optional `HTB_ENV_FILE` via `helpers/env-tokens.js` after each refresh. Load order: **file first**, then `.env`. Invalid refresh → `HtbAuthError` (bot stays online; captains DM'd).

Member profile 404 on optional paths: `isOptionalMemberProfilePath()` — swallow, don't fail bulk sync.

Pusher fallback: `getRecentMemberActivities(memberIds, sinceMs)` — per-member `user/profile/activity`, not `team/activity`.

## htb-api.js patterns

- All requests via `htbApiGet(endpointPath, parseText, { base })`
- Throttle per endpoint key (`getThrottleEndpointKey`); respect `x-ratelimit-*` headers
- Rate-limit waits logged at `info` when wait exceeds threshold
- Machine list: v5 paginated (`MACHINE_LIST_PAGE_SIZE=100`), profiles: v4 selective enrichment
- Normalize v5 machines with `normalizeV5Machine()` before storing
- Paginated responses: `extractPaginatedItems(response)` handles `data`/`message`/`info`

## SevenDatastore sync

```javascript
// Section order (never reorder without updating logs)
["machines", "fortresses", "endgames", "prolabs", "tags", "team", "challenges"]

// Options — see helpers/htb-sync-engine.js (HtbSyncEngine.runPlan)
update()              // missing sections only (partial bootstrap)
update({ delta: true }) // hourly: catalog deltas + stale + new team members
update({ force: true }) // delta catalogs + full team refresh
update({ full: true, bootstrap: true }) // admin clear cache
update({ sections: ["machines"] }) // explicit section delta
```

- `force` ≠ `full`: force delta-syncs catalogs + refreshes all team members; full bootstraps from empty cache
- `getMemberSyncDependencies()` auto-adds machines/challenges/fortresses/endgames/prolabs if missing
- On-demand: `ensureCachedTarget()` / `resolveEntWithEnsure()` — never full-section sync for Pusher
- Specials stored in `MISC.FORTRESSES`, `MISC.ENDGAMES`, `MISC.PROLABS`
- After update: `syncDbExportFields()` + `updateCache()` in bot.js

## Adding a new HTB endpoint

1. Add method to `HtbApiConnector` in `htb-api.js`
2. Normalize response to match existing cache shape (see `api-classes.js` typedefs)
3. Wire into appropriate `update()` phase or on-demand getter in `SevenDatastore`
4. Add progress logging with `logBatchProgress()` for bulk fetches

## Env tuning

| Variable | Default | Purpose |
|----------|---------|---------|
| `HTB_LOG_PROGRESS_EVERY` | 25 | Bulk fetch progress interval |
| `HTB_MACHINE_PROFILE_CONCURRENCY` | 3 | Parallel profile fetches |
| `HTB_API_LOG_REQUESTS` | false | Verbose request logging |
| `HTB_RATE_LIMIT_WAIT_THRESHOLD_MS` | 3000 | When to log rate-limit waits |
